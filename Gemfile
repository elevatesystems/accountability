source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }
gemspec

# Declare any non-development dependencies in the accountability.gemspec file.

group :development do
  gem 'sqlite3'
  gem 'yard', require: false
end
